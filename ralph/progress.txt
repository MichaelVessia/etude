# Ralph Progress Log

## Feature: RPC Client Tests
## Branch: ralph/rpc-client-tests

---

### 2026-01-11 - US-001 Complete

**Status:** US-001 (RPC test utilities) was already implemented.

**Files:**
- `packages/client/src/runtime/__tests__/test-utils.ts` (188 lines)

**What exists:**
- `makeRpcTestLayer(rpcs, responses, url)`: Creates a test layer for RPC clients with mocked HTTP responses. Supports both success and error mocking via `MockRpcResponse` type.
- `makeMockNetworkErrorLayer(errorType)`: Creates layers that simulate network failures (timeout, 500, malformed JSON).
- `makeRpcNetworkErrorLayer(errorType, url)`: Combines network error simulation with RPC protocol layer.

**Verification:**
- Typecheck passes: `bun run typecheck`
- All 331 tests pass: `bun test`

**Next:** US-002 (Test SessionRpcClient methods) is the next priority.

---

### 2026-01-11 - US-002 Complete

**Status:** US-002 (Test SessionRpcClient methods) complete.

**Files created:**
- `packages/client/src/runtime/__tests__/SessionRpcClient.test.ts` (340 lines)

**Files modified:**
- `packages/client/src/runtime/__tests__/test-utils.ts`: Fixed response format to use proper ResponseExitEncoded structure with Schema.ExitEncoded (Exit/Failure with cause). Changed URL from relative `/rpc` to absolute `http://localhost/rpc` but then refactored to use Protocol.make directly, bypassing HTTP altogether.

**Test coverage (10 tests):**
- `startSession`: encodes payload, decodes SessionStarted, handles SessionError (AlreadyActive), handles PieceNotFound
- `submitNote`: encodes note data, decodes NoteResult (correct/wrong/extra), handles SessionError (NotStarted)
- `endSession`: decodes SessionComplete with all fields, handles optional hand accuracy as None, handles SessionError (NotStarted)

**Key fix:** The mock protocol was sending `{ _tag: "Success", ... }` format but RpcClient expects `{ _tag: "Exit", requestId, exit: { _tag: "Success", value } }` format (ResponseExitEncoded). Fixed this for both success and error paths.

**Verification:**
- Typecheck passes: `bun run typecheck`
- All 341 tests pass: `bun test`

**Next:** US-003 (Test PieceRpcClient read methods) is the next priority.

---

